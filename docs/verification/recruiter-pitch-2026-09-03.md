# Recruiter pitch verification — 2026-09-03

## Result

The opt-in Playwright recording completed all eleven scenes in one isolated Chromium
session: **1 passed in 5.0 minutes**. It used a disposable UUID-named SQLite database
and did not read or change the running application's database.

The final video follows the requested timeline: problem and product; architecture;
suspicious transaction; structured explanation; legitimate high-value education
payment; simulator and counterfactuals; bounded investigation and policy retrieval;
human review and audit; held-out metrics and business cost; security; and the
PostgreSQL enum failure and fix.

## Truthful substitutions

- Deployed-model explanations are described as signed local contributions and
  structured evidence, not SHAP.
- The local investigator is described as deterministic orchestration with lexical
  policy retrieval, not a production LLM service.
- Business values are modeled costs/exposure, not confirmed prevented loss.
- The visible transaction and identities are explicitly fictional.

## Media checks

- Final: `outputs/submission/razorshield-recruiter-pitch-final-v2.mp4`
- Duration: **300.000 seconds**
- Video: H.264, 1440 × 1000, 25 fps, yuv420p
- Audio: AAC, 48 kHz mono; mean **-16.4 dB**, peak **-1.4 dB**
- Size: **24,975,723 bytes**
- SHA-256: `c68fde1fa654f7e9b74e937372aeab1e049da4d39349fada5a44a22538959f1a`
- Subtitle streams: none; no bottom narration strip is injected
- Full FFmpeg audio/video decode: passed with no errors
- Narration timing: the seven central chapters use between **25.8 and 49.0 seconds**
  of their 30-to-50-second visual windows; the largest gap was reduced from 26.4
  seconds to 4.2 seconds.
- Visual QA: dashboard, architecture, scoring, explanation, low-risk context,
  counterfactuals, investigation, audit, business-cost, security, and closing frames
  were inspected.

The MP4 and QA images are ignored local outputs and have not been uploaded to an
external video host or Git repository.
